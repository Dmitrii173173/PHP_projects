1. Потому что мы хотим чтобы у нас был один и тот же id в массиве, по которому мы передаём данные.
2. Чтобы постоянно не переписывать код под новую структуру массива
3. Поля в массиве должны быть элементы не связанные с вёрсткой, а должны быть связанны с персональной информацией об объекте,
    а название массива должно быть легко читаемым и понятным.
4. чтобы не портить структуру страницы.
5. сделаю if else
6. foreach($items as $item) {} лучше использовать когда это нужно вывести на всей оставшейся странице
    foreach($items as $item): endforeach; когда нам нужно вывести массив только в определённом месте
7.

                                    <?php if($item['is_active']):?>
                                <li class="breadcrumb-item"><a href="<?php echo $item['href']?>"><?php echo $item['name']?></a></li>
                                    <?php else:?>
                                <li class="breadcrumb-item active"><a href="<?php echo $item['href']?>"><?php echo $item['name']?></a></li>
                                    <?php endif;?>

Такой подход нужен чтобы была возможность менять условия и было менньше кода при условии что условий будет много.
Также чтобы небыло путаницы в коде.